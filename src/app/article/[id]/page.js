import Navbar from "@/components/modules/navbar/Navbar";import Footer from "@/components/modules/footer/Footer";import {authUser} from "@/utils/serverHelpers";export default async function Page({params}){await params;const user=await authUser();return <><Navbar isLogin={Boolean(user)}/><article className="container" style={{padding:"140px 20px 70px",direction:"rtl",lineHeight:2}}><h1>مقالات قهوه</h1><p style={{marginTop:24}}>مدل مدیریت مقاله در سورس اولیه تعریف نشده است. این مسیر برای اتصال به مدل مقاله آماده شده است.</p></article><Footer/></>}
