import Navbar from "@/components/modules/navbar/Navbar";import Footer from "@/components/modules/footer/Footer";import Product from "@/components/modules/product/Product";import ProductModel from "@/models/Product";import {authUser} from "@/utils/serverHelpers";import connectToDB from "@/configs/db";
export default async function Page(){await connectToDB();const[user,products]=await Promise.all([authUser(),ProductModel.find({}).sort({_id:-1}).lean()]);return <><Navbar isLogin={Boolean(user)}/><main className="container" style={{padding:"140px 0 60px",direction:"rtl"}}><h1 style={{marginBottom:30}}>فروشگاه</h1><section style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(250px,1fr))",gap:24}}>{products.map(p=><Product key={String(p._id)} {...JSON.parse(JSON.stringify(p))}/>)}</section>{!products.length&&<p>محصولی ثبت نشده است.</p>}</main><Footer/></>}
