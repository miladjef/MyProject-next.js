import Navbar from "@/components/modules/navbar/Navbar";import Footer from "@/components/modules/footer/Footer";import Stepper from "@/components/modules/stepper/Stepper";import {authUser} from "@/utils/serverHelpers";export default async function Page(){const user=await authUser();return <><Navbar isLogin={Boolean(user)}/><Stepper step="complate"/><main className="container" style={{padding:"50px 20px",direction:"rtl"}}><h1>تکمیل سفارش</h1><p style={{marginTop:20}}>پس از اضافه شدن ماژول سفارش و پرداخت، نتیجه تراکنش در این بخش نمایش داده می‌شود.</p></main><Footer/></>}
