import Breadcrumb from "@/components/modules/breadcrumb/Breadcrumb";import Footer from "@/components/modules/footer/Footer";import Navbar from "@/components/modules/navbar/Navbar";import Product from "@/components/modules/product/Product";import styles from "@/styles/wishlist.module.css";import {authUser} from "@/utils/serverHelpers";import Link from "next/link";import {FaRegHeart} from "react-icons/fa";import WishlistModel from "@/models/Wishlist";
const Page=async()=>{const user=await authUser();const wishes=user?await WishlistModel.find({user:user._id}).populate("product","name price score img").lean():[];const valid=wishes.filter(w=>w.product);return <><Navbar isLogin={Boolean(user)}/><Breadcrumb route="علاقه مندی ها"/><main className={styles.container} data-aos="fade-up"><p className={styles.title}>محصولات مورد علاقه شما</p><section>{valid.map(w=><Product key={String(w._id)} {...w.product}/>)}</section></main>{!valid.length&&<div className={styles.wishlist_empty} data-aos="fade-up"><FaRegHeart/><p>محصولی یافت نشد</p><span>هنوز محصولی در فهرست علاقه مندی‌های شما وجود ندارد.</span><div><Link href="/category">بازگشت به فروشگاه</Link></div></div>}<Footer/></>};export default Page;
